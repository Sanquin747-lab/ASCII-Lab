#!/bin/sh
# Keep the release branch behind review; applies to every configured remote.
while read local_ref local_sha remote_ref remote_sha
do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Direct updates to main are blocked. Push a feature branch and open a pull request." >&2
    exit 1
  fi
done
exit 0
