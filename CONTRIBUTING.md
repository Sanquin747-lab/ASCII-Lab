# 开发、审核与发布

## 分支

- `main`：稳定版，供 GitHub Pages 发布。
- `feature/tape-rain`：磁带雨迭代，通过草稿 PR 展示预览。
- 后续功能用 `feature/<名称>`，修复用 `fix/<名称>`，维护用 `chore/<名称>`。

开始前拉取远端状态，检查当前分支和未提交改动。基于最新稳定版新建分支；已有功能继续在对应分支开发。

## 工作流程

1. 在功能分支实现并提交可说明的小步变化。
2. 验证相关输入、视觉效果、暂停、素材切换和导出；界面改动检查桌面与手机布局。
3. 创建或更新指向 `main` 的草稿 PR，说明功能、验证结果与限制。
4. 由用户确认发布后，才合并 PR；“继续开发”不代表允许上线。
5. 合并后核对 Pages 部署，再提供正式访问地址。需要发布版本号时，根据实际发布内容创建版本标签。

不要直接向 `main` 推送功能，也不要用 GitHub 文件 API 绕过 PR。恢复稳定版用新增的回退提交，不强推、不删除共享历史。

## 本地防误推

克隆后运行：

```sh
git config core.hooksPath .githooks
```

仓库中的 `pre-push` 钩子会拒绝直接更新或删除远端 `main`。它只约束启用钩子的本地 Git，不替代 GitHub 的服务端分支保护，网页和 API 操作仍须遵循上述规则。

建议管理员在 GitHub 为 `main` 启用要求 PR、禁止强推和禁止删除的规则。规则是否启用应在设置中实际核对，不能仅凭本文件视为生效。

## 文件与预览

仓库根目录 `index.html` 是唯一网站源文件，直接打开或使用本机 HTTP 服务预览。导出的素材、录屏、浏览器缓存和压缩包不提交。

稳定包与功能预览包分别标明分支和提交版本。功能预览不复用正式站点地址，审核通过前不改动 Pages 发布分支。

## 2026-09-10 历史修复

原始稳定应用为 `b735d8a`。此前磁带雨提交 `2369818`、`8e4af10` 已保留。修复会在 `main` 新增回退提交，并在其后将磁带雨改动重新应用到功能分支，确保 PR 显示完整功能差异且将来能够正常合并。
